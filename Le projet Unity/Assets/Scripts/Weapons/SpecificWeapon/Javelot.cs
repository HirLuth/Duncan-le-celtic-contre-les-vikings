using UnityEngine;

namespace Weapons.SpecificWeapon
{
    public class Javelot : MonoBehaviour
    {
        [SerializeField]
        
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
